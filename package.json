{
  "name": "accounting-assistant",
  "type": "module",
  "scripts": {
    "setup": "tsx scripts/setup.ts",
    "provision": "tsx scripts/provision.ts",
    "dev": "wrangler dev",
    "deploy": "tsx scripts/deploy.ts",
    "deploy:worker": "tsx scripts/deploy.ts worker",
    "deploy:dashboard": "tsx scripts/deploy.ts dashboard",
    "deploy:ci": "tsx scripts/deploy.ts all --ci",
    "deploy:staging": "wrangler deploy -e staging --minify",
    "infra:plan": "tsx scripts/provision.ts --plan",
    "infra:apply": "tsx scripts/provision.ts --apply",
    "infra:import:d1": "tsx scripts/import-d1.ts",
    "infra:destroy": "tsx scripts/destroy.ts",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest"
  },
  "dependencies": {
    "@google/genai": "^1.46.0",
    "grammy": "^1.41.1",
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "@cloudflare/workers-types": "^4.20260317.1",
    "@types/prompts": "^2.4.9",
    "prompts": "^2.4.2",
    "tsx": "^4.21.0",
    "vitest": "^4.1.1",
    "wrangler": "^4.4.0"
  }
}
