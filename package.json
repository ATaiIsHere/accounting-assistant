{
  "name": "accounting-assistant",
  "type": "module",
  "scripts": {
    "setup": "tsx scripts/setup.ts",
    "provision:account": "tsx scripts/provision-account.ts",
    "bootstrap:invite": "tsx scripts/create-bootstrap-invite.ts",
    "migrate:local": "wrangler d1 migrations apply DB --local",
    "migrate:remote": "wrangler d1 migrations apply DB --remote",
    "migrate:staging": "wrangler d1 migrations apply DB --remote -e staging",
    "dev": "wrangler dev",
    "deploy": "tsx scripts/deploy.ts",
    "deploy:staging": "wrangler deploy -e staging --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest",
    "test:unit": "vitest --config vitest.unit.config.ts"
  },
  "dependencies": {
    "@google/genai": "^1.46.0",
    "grammy": "^1.41.1",
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "@cloudflare/workers-types": "^4.20260317.1",
    "@types/prompts": "^2.4.9",
    "prompts": "^2.4.2",
    "tsx": "^4.21.0",
    "vitest": "^4.1.1",
    "wrangler": "^4.4.0"
  }
}
